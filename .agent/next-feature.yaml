next_feature:
  title: Bring rollback and hash verification parity to install-skill.sh
  evidence: The Unix installer still replaces its destination with rm -rf and does not verify the copied file tree.
  autonomy: recommend_only
  reason: This iteration explicitly excluded Unix installer changes, and the requested Windows vertical slice is complete.
